enum NetworkException {
  noInternetConnection,
  timeOutError,
  cancelled,
  badCertificate,
  unknown,
}

enum HttpException {
  unAuthorized,
  forbidden,
  notFound,
  conflict,
  internalServerError,
  badRequest,
  tooManyRequests,
  unknown,
  unprocessableEntity,
}
